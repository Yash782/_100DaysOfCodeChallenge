package Day_52;
//https://leetcode.com/problems/linked-list-cycle/

//This code dosen't work here it is just what I did on leetcode
public class LinkedListCycle {
    /*public boolean hasCycle(ListNode head) {
        ListNode fast = head;
        ListNode slow = head;

        while(fast != null && fast.next!=null){
            fast = fast.next.next;
            slow = slow.next;

            if(fast == slow){
                return true;
            }

        }
        return false;
    }*/
}
